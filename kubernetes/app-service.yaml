apiVersion: v1
kind: Service
metadata:
  name: coffeequeue-app
  labels:
    app: coffeequeue-app
spec:
  type: ClusterIP
  selector:
    app: coffeequeue-app
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080

